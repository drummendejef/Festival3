<UserControl x:Class="Festival.view.Settings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <UserControl.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Save" Executed="CommandBinding_Save" />
    </UserControl.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="14*"/>
        </Grid.RowDefinitions>

        <!--Uitleg over Settings tablad-->
        <TextBlock HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="Wrap" Text="Hier kunnen algemene instellingen aangepast worden." VerticalAlignment="Top" Width="380" Grid.ColumnSpan="3" Height="20"/>

        <!--Festivalnaam-->
        <Label Content="Festivalnaam" Margin="10,5,10,243" Grid.Row="1"/>
        <TextBox Height="23" Margin="10,30,10,0" Grid.Row="1" TextWrapping="Wrap" Text="{Binding Festivalnaam}" VerticalAlignment="Top"/>
        <Button Content="Opslaan" Margin="10,65,10,197" Grid.Row="1" Command=""/>

        <!--Beroepen toevoegen-->
        <Label Content="Beroep toevoegen" Grid.Column="1" Margin="10,5,10,0" Grid.Row="1" VerticalAlignment="Top"/>
        <TextBox Height="23" Margin="10,30,10,0" Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" Text="{Binding Beroep}" VerticalAlignment="Top"/>
        <Button Content="Opslaan" Margin="10,65,10,197" Grid.Row="1" Grid.Column="1" Command="ApplicationCommands.Save"/>


    </Grid>
</UserControl>
